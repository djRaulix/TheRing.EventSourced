@using System.Web.UI.WebControls
@using WebSample.ReadModel
@{
    ViewBag.Title = "Home Page";
    string value = "toto";
}

<div>
    <br />
    @using (Html.BeginForm("AddAdress", "Home"))
{
    <input type="text" name="adress">
    <input type="submit" name="submit" />
    }
    
</div>

<div>
    Adresses : <br />
    @foreach (var address in Database.Adresses)
    {
        <div><b>@address</b></div>
    }
</div>
